// features/messaging/bale/commands/startCommand.ts

export async function startCommand(): Promise<string> {

  return `سلام 👋

به HamAI خوش آمدی.

دستورات موجود:

/help
/drive

پیامت را ارسال کن تا پاسخ بدهم.`
}
